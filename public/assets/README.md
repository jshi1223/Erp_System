# Assets Structure

- `css/` -> shared stylesheets (`admin-style.css`, `login.css`, `unified-style.css`, `user-style.css`)
- `js/` -> client scripts (`app.js`, `auth-guard.js`)
- `img/` -> static images (`kvsk-logo.jpg`)

All HTML pages now reference files from `public/assets/*`.
